name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  frontend:
    name: Frontend (Vite) — lint, type-check, build, test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["22.13.0"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (frontend)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint frontend
        run: npm run lint:frontend

      - name: Type-check frontend
        run: npm run type-check:frontend

      - name: Build frontend
        run: npm run build:frontend

      - name: Run frontend tests
        run: npm run test:frontend -- --coverage

      - name: Upload frontend artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/

  backend:
    name: Backend (Next.js) — lint, type-check, build, test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["22.13.0"]
    needs: frontend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (backend)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint backend
        run: npm run lint:backend

      - name: Type-check backend
        run: npm run type-check:backend

      - name: Build backend
        run: npm run build:backend

      - name: Run backend tests
        run: npm run test:backend

      - name: Upload backend artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: ./.next/

  mock-server:
    name: Mock Server (NestJS) — lint, build, test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["22.13.0"]
    needs: backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (mock-server)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint mock-server
        run: npm run lint --workspace react-gift-mock-server

      - name: Build mock-server
        run: npm run build --workspace react-gift-mock-server

      - name: Run mock-server tests
        run: npm run test --workspace react-gift-mock-server

      - name: Upload mock-server build artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: mock-server-dist
          path: react-gift-mock-server/dist/
