import { colors, semanticColors } from '@/shared/styles/colors';
import { typography } from '@/shared/styles/typography';
import { spacing } from '@/shared/styles/spacing';

export const theme = {
  colors,
  semantic: semanticColors,
  typography,
  spacing,
} as const;