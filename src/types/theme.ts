export interface ThemeInfo {
  themeId: number;
  name: string;
  title: string;
  description: string;
  backgroundColor: string;
}

export type Theme = {
  themeId: number;
  name: string;
  image: string;
};
